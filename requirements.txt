# Project dependencies for openrouter_ocr_gui.py
# Pin versions for reproducibility.

# HTTP client
requests==2.32.3

# requests dependencies
certifi==2025.1.31
charset-normalizer==3.4.2
idna==3.10
urllib3==2.4.0

# Image loading / preview in Tkinter
Pillow==10.4.0

# Notes:
# - tkinter is part of the Python standard library (ships with most Python distributions).
# - json, base64, threading, os, datetime are standard library modules.
